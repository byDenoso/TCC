name: NEXO Execution Tests

on:
  push:
    paths:
      - 'runtime/nexo_execution/**'
      - '.github/workflows/nexo-execution-tests.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run unit tests
        run: python -m unittest runtime.nexo_execution.test_execution -v
